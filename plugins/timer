#!/usr/bin/env bash
. $PLUGIN_LIB

TIMER_FILES=( $(find $DU_DIR -name "timer-*.json") )

on-initialize() {
  if [ ${#TIMER_FILES[@]} -gt 0 ]; then return $PLUGIN_ENABLED; fi
  return $PLUGIN_DISABLED
}

main "$@"
exit 0
