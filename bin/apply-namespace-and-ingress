#!/usr/bin/env bash
set -euo pipefail

NAMESPACE_YAML=$(mktemp -p $DU_DIR ns-XXXX.yaml)
cat $DU_DIR/$PRODUCT.yaml | envsubst > $NAMESPACE_YAML
cluster-fox kubectl $AVAILABILITY_ZONE -- apply -f $NAMESPACE_YAML
